# This is a Databricks asset bundle definition for lab7_cicd.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: lab7_cicd
  uuid: f470c0ec-ab56-4814-9467-e1270261c909

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405605185991044.4.azuredatabricks.net
    variables:
      catalog: hrynchuk_test
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://adb-7405605185991044.4.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/squ14sss@softserve.academy to make sure we only have a single copy.
      root_path: /Workspace/Users/squ14sss@softserve.academy/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: hrynchuk_test
      schema: prod
    permissions:
      - user_name: squ14sss@softserve.academy
        level: CAN_MANAGE
